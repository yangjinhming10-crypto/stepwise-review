---
name: stepwise-review
description: "Use when the user invokes /stepwise-review with a requirement or task: decompose it into steps, review the plan with an independent subagent, then execute step by step where each step is implemented by one fresh subagent and independently reviewed by another fresh subagent (with cumulative regression checks) before the next step may start. The main conversation only orchestrates — it never implements or reviews itself. Single-step low-risk tasks may auto-select a lightweight mode (implementer + independent reviewer only) with user confirmation; the full mode keeps every gate. --keep retains the run directory incl. the ledger after completion (default: cleaned up)."
disable-model-invocation: true
argument-hint: "需求或任务描述；--focus \"风险面\" 注入审查重点；--commit yes|no 预设基线模式；--light/--full 指定模式；--keep 完成后保留运行台账"
---

# Stepwise review（逐步子代理执行 + 独立审查）

对本次需求采用本流程。核心不变量：**主对话只做编排**——不亲自实现、不亲自审查；
计划、每一步实现、整体产出全部由独立子代理审查，审查通过前不得推进。

## 调用形式

```text
/stepwise-review <需求或任务描述>
/stepwise-review --focus "性能与内存" <需求或任务描述>
/stepwise-review --commit yes <需求或任务描述>
/stepwise-review --light | --full <需求或任务描述>
/stepwise-review --keep <需求或任务描述>
/stepwise-review            （缺省时先向用户询问任务内容）
```

`--focus` 指定的风险面注入**所有**审查子代理（计划审查、逐步审查、终审）的提示词，
作为追加关注项；不替代基础检查项。`--commit yes|no` 预设 1c 的基线模式，免掉确认
时的那一问。`--light` / `--full` 强制指定执行模式（见 1a）；`--light` 不满足单步
硬条件时忽略并告知，按完整模式执行。`--keep` 要求完成后保留运行目录（含
台账）——默认完成后清理运行目录（见 §2 落盘生命周期）。

## 效率原则

质量住在**审查者的独立复验**里。因此一切效率优化只允许动五样东西：上下文传递、
阅读范围、命令调度、打回粒度、步骤间时间安排（并行交叠）——**不得减少所选模式的任何关卡（1a 轻量模式的显式例外除外），
不得削弱任何审查者的独立性与复验义务**。实现者一侧的提速（定向修复、少跑命令）
是安全的，因为重审关卡会独立复验。

## 流程

术语约定：**控制器**即主对话（同一角色的两种称谓，只编排、不实现、不审查）；
**一轮 = 一次审查结论**。计划审查、逐步审查、终审闭环的"连续两轮不过"
均按此计——初审加一次复审仍不过即升级，不存在第三轮打回。

### 1. 拆步

把需求拆成步骤清单并**标注依赖关系**：默认按依赖线性排列；互不依赖的兄弟步骤
（如步骤 2、3 都只依赖步骤 1，彼此独立）**显式标注为可并行槽**，供 §2 并行条件
裁决。每步包含：

- 做什么、动到哪些范围（文件 / 模块 / 产出物）；
- 可判定的验收标准——能被第三方独立核验的表述，不接受"基本完成""基本可用"。

若干步骤共享同一原子改动、无法独立验收时，合并为一步并说明原因；不为凑数拆假
步骤，也不为凑轻量准入把多步任务假合并成一大步——轻量资格存疑按完整模式走。
**依赖关系存疑时按有依赖处理**。用任务清单维护步骤与状态。

### 1a. 模式判定（轻量 / 完整）

拆步完成后判定执行模式：

- **轻量模式**，仅当三个条件同时满足：拆步结果为**单步**；改动范围小（通常
  ≤2 个文件——以拆步声明的涉及范围计，超限或存疑按完整模式——且无跨模块
  接口或契约变更）；验收标准可判定。轻量省略 1b 计划审查与终审，
  **保留**：基线记录、实现子代理、独立审查子代理（判定表 + 证据 + 亲自复跑 +
  三类发现 + 证伪姿态 + 两轮打回上限）。实现者与审查者两道关卡是质量底线，
  任何模式不得低于此。轻量省略第 3 节终审后，收尾报告由控制器直接承接：汇总
  实现者摘要、判定表（审查者判定的原表，控制器不改写结论）、修复记录、
  **未验证项**与验证命令清单（保留副作用标注）呈报用户，呈报后按
  §2 落盘生命周期收尾清理。
- **完整模式**：其余一切情况，全关卡一个不少。
- 缺省由控制器判定，**判定结果与理由并入 1c 的确认消息**（如"单步、改动范围小，
  建议轻量模式"），用户一个词即可否决——用户确认是轻量模式的风险闸门，不得
  省略。**用户否决轻量（或指定 `--full`）时，回退完整路径：先执行 1b 计划审查，
  通过后连同审查结论重新呈请确认**，不得跳过 1b 直接开跑。`--light` / `--full`
  跳过判定直接指定（`--light` 不满足单步硬条件时忽略并告知）。
- 轻量执行中，审查揭示任务实际为多步或高风险（越界涉及多模块、暴露隐藏依赖、
  触碰核心契约）时：停止并升级用户，建议改走完整模式，已产出的审查记录保留；
  不得硬着头皮在轻量模式里修完。用户同意升级后的衔接：**已通过审查的产出保留
  为基线**（其判定表与验证命令计入登记表，记为已完成步骤），控制器对剩余工作
  重新拆步并走 1b → 1c 全流程，此后按完整模式执行。

### 1b. 计划审查（独立子代理）

另开一个全新的只读子代理，只给它**原始需求 + 完整步骤清单**（含 `--focus`
风险面，见调用形式），按五条审：

1. **覆盖完整性**：步骤合起来完整满足原始需求，没有无人认领的要求；
2. **顺序与依赖**：先后关系成立，没有隐含依赖被排反；依赖标注与代码实际情况
   一致，可并行槽不存在隐藏依赖；
3. **原子性与边界**：步骤改动范围边界清晰；确有重叠时必须显式声明并安排串行，
   不得隐式踩踏。并行候选先确认改动**零交集**，语义隔离交由 §2 的**契约抽验**
   裁决——文件不重叠不等于语义独立（改 API 定义与新增调用该 API 的文件零交集
   但强耦合）；
4. **验收标准可判定性**：每条标准可被第三方独立核验，否则该条不合格；
5. **范围纪律**：没有需求之外的步骤。

发现由主对话修订清单后**重开新审查子代理**复审；连续两轮仍有未解决发现、或暴露
需求本身有矛盾，连同审查记录升级给用户，不得开始执行。

### 1c. 用户确认与基线模式

把修订后的清单**连同模式判定（1a）与计划审查结论**（轻量模式无此项）呈给用户
确认，同时问一个问题：

> git 仓库：是否允许每步做任务域 commit？（推荐允许：每步 diff 干净、失败可整步回滚）

记录答案为基线模式，贯穿全部步骤；非 git 任务无此问，固定用快照模式。命令行已用
`--commit` 预设时跳过此问，确认消息一次带全。

### 2. 逐步执行

每步开始前按基线模式记录基线：

- **git + 允许提交**：记录当前 `HEAD` 与既有工作区改动；若既有改动与本步范围重叠、
  无法产出干净的任务域 commit，先向用户报告再继续。实现者完成时产出**任务域
  commit**（不得吸收无关改动）；本步 diff 即该 commit 范围；本步放弃或升级时仅
  revert 该提交回滚，不触碰无关改动；revert 与后续已叠加的 commit 冲突时停止并
  升级，不强行回滚。
- **git + 不允许提交**：记录当前 `HEAD` 与工作区改动清单，本步 diff 相对两者界定；
  既有改动与本步范围重叠时先向用户报告。该模式**无 commit 可回滚**，故每步开始前
  额外把本步将改动的既有文件快照到 `<运行目录>/snapshots/<步骤>/`（运行目录见下方
  落盘生命周期；同快照模式），
  本步放弃或升级时从快照恢复步前状态（该步产出如有价值另存副本），1a 轻量升级的
  "保留为基线"以该快照为锚点；残留的临时改动按用户指示清理。
- **非 git（快照模式）**：本步开始前把将改动的既有文件快照到
  `<运行目录>/snapshots/<步骤>/`（ignore 确认与构建监听 / 全目录扫描时的改放
  例外统一见落盘生命周期），审查者对
  快照 diff；收尾清理按落盘生命周期执行。实现者必须
  报告**全部**触碰文件；触碰了未快照的既有文件时：与本步任务无关、或实现者
  隐瞒未报的触碰一律按**越界**打回；仅当该文件与本步目标直接相关且实现者如实
  报告时，才由控制器记录在案后将该文件**按新增文件整体审查**（原基线已不可得，
  同场景 14 口径），不得无基线糊弄。

控制器维护一张**验证命令登记表**：每步实现者返回、审查者复跑过的可复现命令，
每条标注其验证的验收标准与副作用（会产生持久副作用的命令——改数据库、发外部
请求——须标明，复跑前确认安全，否则按未能验证项处理）；同时按步记录**实际影响
符号集**（实现者报告、以审查者判定表核实为准），累积回归据此按符号交集选命令。
修复轮直接从表里取命令，不重新推导。登记表与各步实现者摘要、判定表、修复记录
与未验证项**追加写入运行台账**（见落盘生命周期），不按步散落多文件——会话中断后
凭台账恢复：报告已完成步骤与在途状态，向用户确认继续方式，不静默从头重做；
在途步骤（实现者已返回、审查未完成）经用户确认后，凭台账中的实现者摘要直接派发
全新审查子代理接续审查，不重复执行实现。

**落盘生命周期**：**1c 确认后**建立运行目录 `tmp/stepwise-review/<运行标识>/`
（运行标识 = 开始日期时间 + ASCII kebab-case 任务简名，撞名追加序号），一次运行
只落两样东西：**单个台账 `ledger.md`**——仅控制器追加，子代理一律以报告返回、
不得写运行目录；头部记录任务描述、模式、基线模式、快照位置（监听例外改放外部
时记绝对路径）、经 1c 确认的步骤清单与各步验收标准（re-plan 回写，见 2e）、
运行状态（进行中 / 已完结 / 已终止；字段缺失或不合法即**状态不明**，最后活动
时间仅作陈旧提示）与最后活动时间（每次追加更新）；之后按步追加登记表、实现者
摘要、符号集、判定表、修复记录与未验证项，条目标步骤号（并行交叠按返回顺序）。
**快照 `snapshots/<步骤>/`**——运行目录整体确认被 git ignore（必要时补
.gitignore）；存在构建监听 / 全目录扫描时改放
`<系统临时目录>/stepwise-review/<运行标识>/snapshots/<步骤>/`（**运行专属
目录**，删除只删该目录本身），台账本体留原地保恢复可发现。每次启动先扫
`tmp/stepwise-review/*/ledger*.md`：进行中 / 状态不明 → 向用户报告在途状态、
确认续接或新建，并校验头部所记外部快照存在性（缺失告知"仅可接续、不可回滚"）；
已完结 / 已终止残留仅提示；台账缺失（tmp 被外部清理）→ 告知无法恢复重新发起，
提示按外部根目录排查孤儿快照。收尾三态：

- **正常完成——默认删除**：收尾报告（完整模式终审闭环后 / 轻量模式控制器承接）
  **呈报时必须已含**验证命令清单（**保留副作用标注**）；呈报即置**已完结**、
  报告注明清理结果，随即先读头部定位删外部快照，再删运行目录。清理中被叫停：
  停止续删、保留剩余并注明（可行时置**已终止**）；被文件锁阻塞（句柄未释放、
  杀毒扫描等）不视为任务失败，注明残留路径供手动清理。
- **正常完成——`--keep` 留档**：留档意图须呈报前以 `--keep` 预设或任一交互点
  说明（**呈报为截止**）；置**已完结**后保留运行目录（含台账）、删全部快照
  （含外部）——快照只是回滚机制，无档案价值；台账改名保留前缀
  `ledger-<任务标识>.md` 并头部标注"快照已清理（含外部）"。
- **异常终止**（会话中断、两轮打回后升级、用户叫停）：**不清理**——落盘为恢复
  与接续而在；可行时置**已终止**，收尾消息注明残留位置，后续由用户处置。

**并行条件**：共享同一物理工作区时，两个子代理并发运行验证命令会互相污染构建
产物与测试结果，本步打回修复也会踩到在途改动——因此交叠派发**仅在运行时提供
独立工作区隔离（如 git worktree：下一步在隔离区实现后合回，合回状态照常走本步
2b 审查——审查对象是合回后的主区状态）时允许**。候选限于拆步标注的**可并行槽**
（依赖图上互不依赖的兄弟步骤），且须满足：基线模式为允许提交、改动范围**零
交集**、**契约抽验通过**——对候选步骤的耦合边界（共享的类型 / 接口 / 数据结构
等）由控制器先行运行一次可执行验证（类型检查、接口测试、schema 校验等），跑过
即认定语义隔离成立，由证据而非主观判定背书；抽验命令与结果记入登记表，re-plan
修订步骤后须重跑；隔离区返回的验证命令一律改写为主区路径后登记，保证合回后凭
登记表可复跑。本步审查一旦不通过，立即暂停后续派发（含隔离区）直至修复
通过。任一条件不满足或存疑，严格串行。并行只改时间安排，每步的实现者、审查者
关卡一个不少。

**审查窗口预热**：某步进入 2b 审查期间，若队列中确有下一步，可同时派发下一步
的实现子代理做**只读预热**：探索代码、产出一份**不落盘的实施计划**（拟改文件、
拟改符号、验证命令草案）。预热代理不得修改任何文件、不得运行会产生写入的命令
（验证命令留给正式实现与审查阶段）；本步审查打回修复时预热产物作废重做；通过
后预热产物并入下一步 2a 的上下文包，把探索成本从写盘阶段剥离到等待窗口。

对每一步依次：

**a. 实现子代理**：开一个具备实现能力的新子代理。上下文包**在派发那一刻从当前状态
重新组装**（禁止复用拆步时的快照），并尽量替子代理省掉从零探索的成本：

- 涉及文件的**绝对路径**与已知的行号 / 符号线索（拆步与前序步骤已知的一切定位信息）；
- 可直接复制运行的**验证命令原文**（登记表里已有的）；
- 相关规范或仓库指引（如 AGENTS.md）的**摘录**，而不是"自行去读"的指令；
- 本步骤内容与验收标准、需保留的现状。

要求它返回：改动说明、**全部触碰文件清单（含计划外文件）**、**实际影响符号集**
（新增 / 修改的函数、类型、接口名）、可复现的验证命令及其输出。控制器将实现者
摘要与符号集追加到运行台账（中断恢复、修复续传与累积回归选命令
用），但不得替它写代码，也不得直接修补它的产出。

**b. 独立审查子代理**：实现者返回后，**另开一个全新的子代理**审查本步：

- 禁止复用实现子代理的对话，禁止把实现者的自评当作审查输入；
- 给它：原始需求、本步骤验收标准、实际改动（按基线模式界定）、`--focus` 风险面；
- **定向阅读**：从 diff 出发，只读 diff 触及的文件及必要的直接上下文，不做宽域
  探索；定位线索直接用上下文包里给的路径与行号。**允许定向追溯**：沿 diff 改动
  符号的引用链（调用方 / 被调用方）做有界探测——实现者漏改下游联动属于"缺失"
  类缺陷，只盯 diff 文件看不见；
- **并行验证**：互不依赖的验证命令在同一轮并行发起；
- **防缓存假绿**：增量构建 / 测试缓存可能掩盖回归，结果可疑时先清理构建产物再
  复跑（属写盘豁免）；
- **输出必须是判定表**：逐条验收标准 → 通过 / 不通过 / 未验证 + 证据（文件行号，
  或审查者**亲自运行**的命令输出；"未验证"即刻转入下列未能验证项规则）；之外
  附三类发现：**缺失**、**越界**、**实现错误**；
- **证伪姿态**：主动构造失败用例、寻找反例，不为放行找理由；
- **累积回归**：按**符号交集**选命令——从登记表取符号集与本步实际影响符号集
  相交（或相交存疑）的前序步骤验证命令重跑，重跑量只与真实重叠相关、不随步数
  线性增长；结果计入判定表；
- **未能验证项**：本地无法核验的标准（缺凭据、要真机 / 外部服务）必须显式列出并
  附原因，不得默默计为通过；未验证项在最终报告中呈报用户，既不当过关依据、也不
  自动拦截；
- 审查者只报告：不得修改源文件与产出物；运行验证命令产生的缓存 / 构建产物属
  豁免，不算违规。

**c. 过关判据与打回**：判定表无"不通过"条目**且**三类发现为空才通过（"未验证"
条目按未能验证项规则呈报，不阻断）。不通过：把**未通过的
判定表条目**与发现**原样**发回**同一个**实现子代理，指示它做最小定向修复、只重跑
受影响的验证命令——重审关卡会独立复验全量相关项，实现者不必自查代劳；允许
提交模式下，修复改动并入本步任务域（追加 commit 或 amend，本步 diff 为该步全部
提交的累计范围）。修复后重走
b（重开新的审查子代理）。同一步连续两轮（初审 + 复审）不通过、或发现需求本身有
矛盾或不可判定，停止并向用户升级，附两轮审查记录。

**d. 通过后**记录"实现者摘要 ↔ 判定表 ↔ 修复记录 ↔ 未验证项"，进入下一步。

**e. 中途 re-plan**：审查暴露的是**计划缺陷**（漏步骤、顺序错、边界划错）而非执行
问题时，不终止任务：暂停队列 → 修订剩余步骤（同步回写台账头部步骤清单与验收
标准） → 对修订部分增量重跑 1b 五条审查 → 呈用户确认 → 从当前进度继续。已完成步骤默认不重做；但依赖顺序错误意味着已完成
产出可能建立在错误前置之上时，须向剩余队列**插入针对已完成产出的修复步骤**。
用户在任意时点变更需求同样走本路径：暂停 → 修订 → 增量 1b → 用户确认 → 继续。

### 3. 终审与汇总

再开一个全新子代理做整体终审，**只给它**：原始需求、全量累计改动、各步验收标准、
`--focus` 风险面、验证命令登记表（可直接复跑取证，不必重新推导）。**不给它逐步
审查结论**（防锚定），要求独立判定整体完整性、
跨步骤一致性、接口自洽；登记表为空（如纯文档任务）时，以产出文件全文对照
各步验收标准做交叉比对。终审不必逐行重推已由逐步关卡覆盖的步骤内部细节——它的
职责是接合部与整体，不是第二遍逐步审查。

终审发现**必须闭环**：每条发现发回其责任步骤的实现者修复，并由新子代理就该发现
复审；终审阶段的修复属于跨步骤改动，复审必须**全量重跑验证命令登记表**做全局
累积回归，涉及核心契约调整时走 2e 增量 re-plan。同样两轮上限，超限升级用户。

最后向用户报告：每步判定表与修复记录、未验证项汇总、终审结论与处置结果、最终
产出位置与验证结果，并附登记表的**验证命令清单（保留副作用标注，台账删除后凭
报告仍可安全复验）**。报告呈报后按落盘生命周期收尾清理（默认删除运行目录，
`--keep` 例外；报告中注明已清理与留档方式）。

## 运行时映射

- 实现者：映射到当前运行时的实现型子代理（如 general-purpose）。
- 审查者（计划审查 / 逐步审查 / 终审）：映射到"源文件只读但**具备命令执行能力**"
  的子代理——部分运行时的只读探索型（如 Explore）带命令执行，可直接用；若该
  环境的只读型**不能运行命令或不适合深度审查**，改用通用型，并在其提示中明令
  "只报告、不修改源文件与产出物"（运行验证命令产生的缓存 / 构建产物豁免）。
- 修复发回：优先复用同一实现子代理继续；运行时不支持恢复子代理对话时，以落盘的
  实现者摘要 + 相关判定表组装上下文新开**等价实现者**（记录该替换，不算新实现者）。

## 红线（任何情况下不得）

- 控制器亲自实现，或亲自审查代替子代理审查——包括"顺便看一眼就算审过"；
- 复用实现子代理（或任一前序审查者）的对话或结论做审查；
- 计划未经 1b 独立审查与用户确认就开始执行（轻量模式例外：仅限单步任务，且
  用户已确认模式判定，见 1a）；
- 审查未通过或未出结论，就放行或合并下一步的**产出**（交叠派发与只读预热本身
  不违规，见并行条件与审查窗口预热；预热写了盘即违规）；或省略终审（轻量模式
  例外，见 1a）；
- 终审发现只报告不处置（必须走修复-复审闭环）；
- 未能验证项默默计为通过；
- 同一步（或同一终审发现）无限打回循环（两轮上限后必须升级）。

## RED/GREEN 场景自检

23 个 RED/GREEN 场景是行为契约样例与变更回归清单，维护在
[docs/SCENARIOS.md](./docs/SCENARIOS.md)——修改本文件后逐条对照；涉及流程 /
关卡 / 红线 / 模式判定 / 落盘生命周期的实质变更，逐条过场景，并建议重跑外部
验收（运行方式见 CHANGELOG 附记）。

## 文档维护

本 skill 的功能与使用文档在 [docs/README.md](./docs/README.md)，变更记录在
[docs/CHANGELOG.md](./docs/CHANGELOG.md)，场景自检在 [docs/SCENARIOS.md](./docs/SCENARIOS.md)。
任何对本文档的修改都必须同步更新这三个文档；涉及流程 / 关卡 / 红线 / 模式判定 /
落盘生命周期的实质变更，建议重跑外部验收（现行三路：omp / opencode / Agy——
DSH 通道已于 2026-09-03 停用；运行方式见 CHANGELOG 附记）。
