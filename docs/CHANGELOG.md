# stepwise-review 变更记录

维护规则：每次修改 SKILL.md 必须在此追加一条记录（日期 + 版本 + 变更摘要 + 是否复验）。

## 2026-09-01 · v1.1 —— 串行效率四项优化（双路独立审查零阻断通过）

1. **拆步产出依赖图（DAG）**：互不依赖的兄弟步骤显式标注"可并行槽"，依赖存疑
   按有依赖处理（§1、§1b.2）。
2. **登记表附加实际影响符号集**：实现者报告、审查者核实；累积回归按符号交集选
   命令，重跑量从随步数线性增长降为仅与真实重叠相关（§2、§2a、§2b）。
3. **审查窗口预热**：某步审查期间下一步实现者只读预热（不落盘的实施计划），
   打回作废、通过并入 2a 上下文包（§2）。
4. **契约抽验准入**：并行候选的语义隔离由耦合边界的可执行验证背书（类型检查 /
   接口测试 / schema 校验），替代主观判定；抽验记入登记表，re-plan 后重跑
   （§1b.3、§2 并行条件）。
5. 场景自检增至 22 个（新增 20 预热 / 21 符号集回归 / 22 契约抽验），红线第 4 条
   补预热口径（预热落盘即违规）。

关卡、独立性、复验义务零改动；四项均落在效率原则"五样东西"之内。
复验：Agy + DSH 双路独立审查，均判定"通过、零阻断项"（各有少量建议级发现，
留待下轮迭代）。

## 2026-08-28 · v1.0 —— 初版定稿（三路独立验收通过）

**当日迭代历史**（同日完成，合并记录）：

1. **v0.1 初版**：核心流程——拆步 → 用户确认 → 逐步（实现子代理 + 独立审查子代理，
   判定表/证据/三类发现）→ 终审；红线五条；场景自检 5 个。
2. **v0.2 补强**（独立评审驱动）：计划审查关卡（五条）、终审闭环处置、累积回归、
   审查输出结构化（判定表 + 证伪姿态 + 未能验证项）、基线操作化（commit 模式 / 快照
   模式）、上下文包派发时现做、中途 re-plan、终审防锚定、`--focus` 参数。
3. **v0.3 效率优化**：定向阅读、并行验证、验证命令登记表、定向打回、受限并行交叠、
   `--commit` 预设；效率原则钉死"只动五样东西，不减关卡"。
4. **v0.4 双审修复**（两个独立评审方各一轮）：审查者写盘豁免、快照越界处置、
   并行条件升级"零交集且语义独立"、revert 冲突处置、2e 插入修复步骤、红线与交叠
   措辞对齐、"轮"统一定义（一轮 = 一次审查结论）、落盘恢复、终审获得登记表、
   定向追溯（引用链探测）、worktree 隔离前提。
5. **v0.5 轻量模式**（用户决策）：1a 模式判定（单步 + ≤2 文件 + 可判定标准自动建议
   轻量）、`--light`/`--full`、否决回退 1b、轻量升级衔接、反假合并禁令。
6. **v0.6 验收修复**（第三评审方阻断项 + 三方建议）：git+不允许提交模式补步前快照
   回滚与恢复程序；判定表三态化；登记表标注验收标准与副作用；防缓存假绿；worktree
   合回关卡；修复轮 commit 范围；用户中途变更需求入口；在途中断恢复接续；终审空
   登记表处置；场景 5/11/14 口径对齐。

**验收结论**：三个独立评审方（内置子代理 + 两个外部 headless 代理）均判定
"通过、零阻断项"。

## 2026-08-28 · v1.0.1 —— 文档体系

新增 `docs/README.md`（功能与使用文档）与 `docs/CHANGELOG.md`（本文件）；
SKILL.md 末尾追加"文档维护"条款。不涉及流程、关卡、红线、模式判定的任何变更，
无需复验。

## 2026-08-28 · v1.0.1-post —— GitHub 发布包

按发布形态整理：仓库根 README（面向 GitHub 首页）、SKILL.md（与验收版字节一致）、
docs/ 双文档（去除开发机私有路径信息）。无 skill 行为变更。

---

### 附：独立评审验收方式（复验时参考）

- 评审方：至少两个互相独立的评审代理（本仓库开发时用了三个：宿主内置子代理、
  以及两个外部 headless 代理）。
- 验收标准五条：**A** 内部一致性（流程/效率原则/红线/场景自检互证无矛盾）；
  **B** 可执行性（中等能力代理只凭文档可无歧义执行）；**C** 关卡完整性（完整模式
  全关卡、轻量保底双关卡、例外严格限定）；**D** 关键失败模式无遗漏；**E** 安全性
  （各基线模式均无数据丢失路径）。
- 结论格式：**通过 / 不通过** + 逐条达标依据；发现分"阻断 / 建议"两级，建议级
  不阻断。有阻断项 → 修复后全部评审方重跑。
