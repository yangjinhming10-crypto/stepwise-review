# stepwise-review 变更记录

维护规则：每次修改 SKILL.md 必须在此追加一条记录（日期 + 版本 + 变更摘要 + 是否复验）。

## 2026-09-02 · v1.2 —— 留档生命周期：单台账 + 完成即清

用户痛点：每次执行后项目 `tmp/` 里过程文件太多太乱，完成后大部分不再使用。

1. **一次运行一个目录**：`tmp/stepwise-review/<运行标识>/`（开始日期时间 + 任务
   简名）；台账头部记录任务描述、模式与基线模式，残留目录可自识别；新运行不写
   其他运行目录，无关残留报告用户、不自动删（§2 落盘生命周期）。
2. **单台账文件**：登记表、实现者摘要、符号集、判定表、修复记录、未验证项全部
   追加写入 `ledger.md`；快照归 `snapshots/<步骤>/`；一次运行落盘 = 1 台账 +
   快照目录，不再按步散落多文件（台账追加点在 §2 与 §2a；两处基线模式的快照
   路径同步改口径，见 §2）。
3. **收尾三态清理**：正常完成（完整模式终审闭环 / 轻量模式收尾报告呈报后）默认
   删除运行目录，删档前把**验证命令清单**并入收尾报告，保证删档后凭报告仍可
   复验；`--keep` 或用户留档要求保留 `ledger.md`（快照仍删——快照只是回滚机制，
   无档案价值）；中断、两轮打回升级、用户叫停等异常终止不清理，收尾消息注明
   残留位置（§1a、§2、§3）。
4. 新增 `--keep` 参数（frontmatter / 调用形式 / README 参数表）；场景自检增至
   23 个（新增 23 留档生命周期），场景 16 措辞对齐（运行目录与台账）。

关卡、独立性、复验义务、模式判定零改动；变更限于落盘布局与收尾清理。
复验：omp + DSH + opencode + Agy 四路独立审查（用户指定），审查对象为 v1.2 初稿，
结论与整合见下方 v1.2.1 条目。

## 2026-09-02 · v1.2.1 —— 四路审查意见整合

四路结论（对 v1.2 初稿）：omp **通过**（建议 8 条）/ opencode **通过**（建议
7 条）/ DSH **不通过**（阻断 1 条 + 建议 10 条）/ Agy **通过**（建议 3 条；
其 run1 read-only 下 command 权限被自动拒绝产出为空、run2 agent 执行异常，
run3 对内容相同的快照副本复跑成功）。阻断项与收敛建议全部收口如下：

1. **外部快照生命周期闭环**（DSH 阻断项 B1；omp S1 / opencode 1 / Agy 1 收敛）：
   台账头部记录快照位置（改放系统临时目录时记绝对路径）；正常完成删运行目录时
   按台账一并删外部快照（`--keep` 同样删全部快照）；恢复凭台账定位快照。
2. **台账头部增强与启动扫描**（omp S4/S5、DSH 4/9、opencode 3）：头部增记快照
   位置、经 1c
   确认的步骤清单与各步验收标准（re-plan 同步更新）、运行状态与最后活动时间
  （甄别活运行与陈旧残留）；每次启动先扫 `tmp/stepwise-review/*/ledger.md`，
   进行中/状态不明者报告用户确认续接或新建，台账缺失则告知无法恢复重新发起。
3. **单写者与并行标记**（omp S7、DSH 5）：台账仅由控制器追加，子代理以报告
   返回不写盘；条目显式标注步骤号；隔离区返回的验证命令改写为主区路径后登记。
4. **`--keep` 落点与时点**（omp S2、DSH 1/3、opencode 6）：留档意图须在收尾
   报告呈报前表达（`--keep` 预设或当场说明）；`--keep` 保留运行目录、仅删全部
   快照（含外部），台账可原地改名带任务标识。DSH 1 的"呈报后等用户确认再删"
   变体**未采纳**：与用户痛点（tmp 要干净）及本 skill 少交互原则冲突，以
   "报告注明已清理与留档方式 + `--keep` 预设"替代。
5. **删除健壮性**（omp S6、DSH 10、Agy 3）：清理顺序固定先快照后台账；清理中
   被叫停即停止并保留剩余（按异常终止口径）；删除被文件锁阻塞（Windows 句柄 /
   杀毒扫描）不视为任务失败，注明残留路径供手动清理。
6. **杂项**：运行标识简名限定 ASCII kebab-case、撞名追加序号（Agy 2 / omp S3 /
   DSH 2 / opencode 7）；ignore 确认上提为运行目录整体前置（opencode 4/5）；
   收尾报告验证命令清单保留副作用标注（DSH 6）；收尾三态拆为三条并列、场景 23
   措辞对齐（opencode 2、DSH 7）；README 补轻量收尾闭环与参数表目录级语义、
   frontmatter description 补 `--keep`（omp S8）；本文件 v1.2 条目 §2a 引用
   歧义修正（DSH 8）。

关卡、独立性、复验义务、模式判定仍零改动。
复验：四路复审全部通过（omp 18 / opencode 7 / DSH 10 / Agy 1 条建议，零阻断），
建议整合见 v1.2.2。

## 2026-09-02 · v1.2.2 —— 四路复审整合（措辞精度与边界拧紧）

四路复审结论（对 v1.2.1 定稿）：omp **通过**（建议 18 条）/ opencode **通过**
（建议 7 条）/ DSH **通过**（建议 10 条）/ Agy **通过**（建议 1 条）——零阻断，
全部为措辞精度、边界约束与文档同步。整合如下：

1. **状态机补全**（DSH 1、omp S12/S14）：状态枚举补**已终止**；收尾报告呈报时
   即置**已完结**（清理尝试前），异常终止可行时置**已终止**；状态字段缺失或
   不在枚举内即状态不明，最后活动时间仅作陈旧提示；扫描对已完结/已终止残留
   仅提示不询问（清理受阻/叫停后的残留状态不再失据）。
2. **清理时序与措辞**（omp S1/S11、DSH 2/3）：收尾报告**呈报时必须已含**验证
   命令清单——替代"删除前并入"的倒置时序表述；清理中被叫停改为"停止续删、
   保留剩余、注明位置"，不再引用异常终止口径；场景 23 异常枚举与三态完全对齐
  （补用户叫停，区分叫停=停止续删）。
3. **`--keep` 语义拧紧**（omp S2/S13/S15、DSH 2/10、opencode 5）：措辞统一
   "保留运行目录（含台账）"（frontmatter description 同步）；留档意图呈报前
   任一交互点表达、呈报为截止；改名保留可扫描前缀 `ledger-<任务标识>.md`，
   扫描 glob 放宽为 `*/ledger*.md`；改名时头部标注"快照已清理（含外部）"；
   收尾报告固定注明清理结果。
4. **外部快照安全边界**（omp S9/S10、DSH 6、opencode 1/6）：钉死运行专属路径
   `<系统临时目录>/stepwise-review/<运行标识>/snapshots/<步骤>/`，删除只删该
   目录本身、不删父目录；删外部快照前先读台账头部定位；启动扫描校验外部快照
   存在性，缺失告知"仅可接续、不可回滚"；台账缺失时提示按外部根目录排查
   孤儿快照。
5. **流程锚点**（DSH 5/7、omp S16/S17、agy 1）：运行目录与台账头部在 **1c
   确认后**建立；2e re-plan 显式回写台账头部；轻量收尾呈报审查者原表（控制器
   不改写结论）并保留副作用标注；场景 16 补台账缺失分支。
6. **文档同步**（omp S3/S4/S5/S6/S7/S8/S18、DSH 4/8/9、opencode 7）：README
   参数表/生命周期段/交互时点/并行条件/基线模式括号同步（--keep 快照仍全删、
   ignore 上提、启动扫描交互点、re-plan 重跑与主区路径改写、异常枚举补叫停、
   小节标签去版本号）；本文件 v1.2.1 条目补 opencode 3 落点与复验行、附记补
   四路说明。

关卡、独立性、复验义务、模式判定仍零改动。
复验：即本轮四路复审（结论见上）；v1.2.2 为措辞与边界整合，如再作实质修订
建议重跑。

## 2026-09-01 · v1.1 —— 串行效率四项优化（Agy/DSH 串行逻辑评估驱动）

1. **拆步产出依赖图（DAG）**：互不依赖的兄弟步骤显式标注"可并行槽"，依赖存疑
   按有依赖处理（§1、§1b.2）。
2. **登记表附加实际影响符号集**：实现者报告、审查者核实；累积回归按符号交集选
   命令，重跑量从随步数线性增长降为仅与真实重叠相关（§2、§2a、§2b）。
3. **审查窗口预热**：某步审查期间下一步实现者只读预热（不落盘的实施计划），
   打回作废、通过并入 2a 上下文包（§2）。
4. **契约抽验准入**：并行候选的语义隔离由耦合边界的可执行验证背书（类型检查 /
   接口测试 / schema 校验），替代主观判定；抽验记入登记表，re-plan 后重跑
   （§1b.3、§2 并行条件）。
5. 场景自检增至 22 个（新增 20 预热 / 21 符号集回归 / 22 契约抽验），红线第 4 条
   补预热口径（预热落盘即违规）。

关卡、独立性、复验义务零改动；四项均落在效率原则"五样东西"之内。
复验：Agy + DSH 独立审查（结果见发布记录）。

## 2026-08-28 · v1.0 —— 初版定稿（三路验收通过）

**当日迭代历史**（同日完成，合并记录）：

1. **v0.1 初版**：核心流程——拆步 → 用户确认 → 逐步（实现子代理 + 独立审查子代理，
   判定表/证据/三类发现）→ 终审；红线五条；场景自检 5 个。
2. **v0.2 补强**（子代理审查驱动）：计划审查关卡（五条）、终审闭环处置、累积回归、
   审查输出结构化（判定表 + 证伪姿态 + 未能验证项）、基线操作化（commit 模式 / 快照
   模式）、上下文包派发时现做、中途 re-plan、终审防锚定、`--focus` 参数。
3. **v0.3 效率优化**：定向阅读、并行验证、验证命令登记表、定向打回、受限并行交叠、
   `--commit` 预设；效率原则钉死"只动五样东西，不减关卡"。
4. **v0.4 双审修复**（ZCode 子代理 + Agy 各一轮）：审查者写盘豁免、快照越界处置、
   并行条件升级"零交集且语义独立"、revert 冲突处置、2e 插入修复步骤、红线与交叠
   措辞对齐、"轮"统一定义（一轮 = 一次审查结论）、落盘恢复、终审获得登记表、
   定向追溯（引用链探测）、worktree 隔离前提。
5. **v0.5 轻量模式**（用户决策）：1a 模式判定（单步 + ≤2 文件 + 可判定标准自动建议
   轻量）、`--light`/`--full`、否决回退 1b、轻量升级衔接、反假合并禁令。
6. **v0.6 验收修复**（DSH 阻断项 + 三方建议）：git+不允许提交模式补步前快照回滚与
   恢复程序；判定表三态化；登记表标注验收标准与副作用；防缓存假绿；worktree 合回
   关卡；修复轮 commit 范围；用户中途变更需求入口；在途中断恢复接续；终审空登记表
   处置；场景 5/11/14 口径对齐。

**验收结论**：ZCode 子代理 ✅ / Agy ✅ / DSH ✅，均判定"通过、零阻断项"
（验收运行：`agyr-stepwise-accept-0828-r2`、`dshr-stepwise-accept-0828-r2`）。

## 2026-08-28 · v1.0.1 —— 文档体系

新增 `docs/README.md`（功能与使用文档）与 `docs/CHANGELOG.md`（本文件）；
SKILL.md 末尾追加"文档维护"条款；全局 AGENTS.md 加维护钩子。
不涉及流程、关卡、红线、模式判定的任何变更，无需复验。

---

### 附：三路验收的运行方式（复验时参考）

- ZCode 子代理：派 general-purpose 后台子代理，验收标准 A–E（一致性/可执行性/
  关卡完整性/失败模式/安全性），结论格式"通过/不通过 + 阻断项"，建议级不阻断。
- Agy：受管 headless（`agy-headless-runner.mjs`，read-only，prompt 存
  `.zcode/tmp/agy-prompts/`，run 记录在 `.zcode/tmp/agy-runs/`）。
- DSH：受管 headless（`dsh-headless-runner.mjs`，同上，`dsh-` 前缀）。
- v1.2 起为四路外部 headless（omp / opencode / DSH / Agy；Agy 用
  workspace-write——read-only 下读文件的 command 权限会被自动拒绝；run 前缀
  `swrv12-*` 与 `swrv121-*`）。
